# _SAIPAL — Audit Handoff

RUN_ID: acb-mtlikhfw-fe84a78fc5a84a868426
GENERATED_AT: 2026-09-03T16:25:51.811239
PROJECT_NAME: SAIPAL
TARGET: SAIPAL_03.09.26-T15-41-02.zip -> extracted SAIPAL project root
BASELINE: VERSION=0.3.0; archive_sha256=85244f6d2ee5462edad804d8c90c8072098a19acfeb458c6fcbd8c90c0b83fb6; inspected_tree_sha256=2933febae97baa9514f5181614987496ac442e0501bbd6b5ed170544b0688339
GIT_CONTEXT: ABSENT - supplied archive contains no .git metadata; no commit/branch identity can be asserted
SAIPEN_CONTEXT: PRESENT - .saipen/STATE.md phase=BUILD task=T-055 next_action="PHASE BUILD T-055"; BOARD contains pending T-061..T-067; live implementation was treated as authoritative and overlapping defects below were independently reverified
TOTAL_TICKETS: 24
STATUS: AUDIT_ALL_3: COMPLETE

## Summary of Wave Audits
- Wave 1 (Core): 10 tickets (baseline: VERSION=0.3.0; archive_sha256=85244f6d2ee5462edad804d8c90c8072098a19acfeb458c6fcbd8c90c0b83fb6; inspected_tree_sha256=2933febae97baa9514f5181614987496ac442e0501bbd6b5ed170544b0688339)
- Wave 2 (Second Wave): 8 tickets (baseline: VERSION=0.3.0; archive_sha256=85244f6d2ee5462edad804d8c90c8072098a19acfeb458c6fcbd8c90c0b83fb6)
- Wave 3 (Performance): 6 tickets (baseline: VERSION=0.3.0; archive_sha256=85244f6d2ee5462edad804d8c90c8072098a19acfeb458c6fcbd8c90c0b83fb6)

---
## 01 — AUDIT CORE
PROJECT_NAME: SAIPAL
DATE_TIME: 2026-09-03T15:41+03:00
CAMPAIGN_PROFILE: quick3
CAMPAIGN_PROFILE_VERSION: 1.0.0
CAMPAIGN_RUN_ID: acb-mtlikhfw-fe84a78fc5a84a868426
CAMPAIGN_MANIFEST_SHA256: 97d9d053e37e6597a8559008911f84b28f799f326a97c69ebc5b304690defa72
WAVE_ID: core
WAVE_INDEX: 1
WAVE_COUNT: 3
WAVE: AUDIT CORE
TARGET: _SAIPAL_03.09.26-T15-41-02.zip -> extracted SAIPAL project root
BASELINE: VERSION=0.3.0; archive_sha256=85244f6d2ee5462edad804d8c90c8072098a19acfeb458c6fcbd8c90c0b83fb6; inspected_tree_sha256=2933febae97baa9514f5181614987496ac442e0501bbd6b5ed170544b0688339
PREVIOUS_WAVE_SHA256: NONE
GIT_CONTEXT: ABSENT - supplied archive contains no .git metadata; no commit/branch identity can be asserted
SAIPEN_CONTEXT: PRESENT - .saipen/STATE.md phase=BUILD task=T-055 next_action="PHASE BUILD T-055"; BOARD contains pending T-061..T-067; live implementation was treated as authoritative and overlapping defects below were independently reverified
AUDIT_SCOPE: CLI _continue_cycle/trigger -> source dispatch/adapters -> bundle validation -> canonical inbox/import -> session identity/generations/protocol binding -> episode analysis/budgets -> finding lifecycle -> historical-rule resolution -> audit enqueue/sink -> closed-loop disposition -> STATE/LOG/index persistence -> HomeLock/SessionLease/AuditInboxLock -> protocol/registry contracts -> targeted regression tests/validator
TEST_STATUS: TEST_FAILED
TEST_LIMITATION: Full canonical runner did not finish within the available execution ceiling: `python -B tools/test_runner.py --json` timed out at 120s and `python -B tools/test_runner.py --family unit --json` timed out at 240s. Relevant targeted tests and deterministic local fault reproductions were run separately.
VERIFIED_INSTEAD: `python -B tools/validate.py` PASS (270 checks); `python -B -m compileall -q tools` PASS; targeted binding tests produced 2 reproducible FAILs; deterministic temporary-home reproductions verified lock ownership loss, event-budget bypass, leaked session lease, cross-generation duplicate generation, enqueue crash duplication, sink retry loss, LOG sequence regression, and disposition-history overwrite
STATUS: AUDIT_CORE: COMPLETE
TICKETS: 10
HANDOFF: IMPLEMENTATION_AGENT
COVERAGE_INSPECTED: README.md; VERSION; SAIPAL_OPERATIONAL_HANDOFF.md; saipal/CORE.md; saipal/SESSIONS.md; saipal/MANIFEST.json; saipal/REGISTRY.json; .saipen/STATE.md; .saipen/BOARD.md; .saipal STATE/config/sources; tools/saipal.py; tools/validate.py; tools/test_runner.py; tools/saipal_engine/{bundle,inbox,sessions,historical,pipeline,hardening,paths,enqueue,sink,closedloop,log,dispatcher,findings,recurrence}.py; relevant tests including test_wave_b_binding.py, test_wave_b_inbox.py, test_wave_a_lock.py, test_wave_a_log.py, test_wave_i_hardening.py, test_wave_e_audit.py, test_wave_sink.py
COVERAGE_DEFERRED: exhaustive provider-adapter normalization branches; exhaustive detector/analyst taxonomy branches; performance/scaling work intended for later Quick3 waves; full unit-suite completion was blocked by execution ceiling
CROSS_WAVE_REFERENCES: NONE_FOR_THIS_CAMPAIGN; pre-existing .saipen TODO T-061..T-066 overlap several independently verified defects below
RESIDUAL_UNCERTAINTY: no Git revision identity was supplied; full unit suite did not complete; Windows-specific filesystem behavior was not executed directly, so CORE-005 filename-safety verification is based on accepted schema plus raw-path construction and must receive a Windows regression test

[P0] [CORE-001] tools/saipal_engine/sessions.py::new_record,binding_status_of / tools/saipal_engine/inbox.py::import_inbox
EVIDENCE:
- `saipal/SESSIONS.md` PAL-SESSION-03 states that `proof_level` is derived by Layer A and is never granted by transcript prose or a bundle's declared `binding_status`.
- `new_record()` lines 192-202 does the opposite: if the incoming bundle already contains any `protocol.binding_status`, it trusts that value and its supplied/default proof instead of calling `binding_proof_of(... authority=...)`.
- `bundle.py` lines 112-124 merely checks declared binding/proof values against enums, so a directly supplied canonical inbox bundle can carry syntactically valid authority claims.
- `binding_status_of()` lines 435-440 also directly returns an existing `binding_status` instead of proving it.
- Targeted project regression test:
  `PYTHONPATH=tools python -B -m unittest -v test_wave_b_binding.BindingStatus.test_declared_status_cannot_self_promote`
  FAILS with `AssertionError: 'BOUND' != 'UNKNOWN'`.
- A second existing test, `test_protocol_binding_persists_in_the_index`, currently FAILS with proof `UNKNOWN` vs expected `RELEASE_REGISTRY`, exposing contradictory old/new test expectations around the same invariant.
DEFECT:
Canonical bundles dropped directly into `.saipal/session_inbox/` bypass dispatcher-side authority verification and can self-promote their persisted session record to `BOUND`. Downstream `_applicable()`/`historical_applicability()` accepts the persisted status, so forensic conclusions can be based on a binding that Layer A never independently established. This violates PAL-BINDING-01/PAL-SESSION-03 and is a fundamental forensic-correctness failure.
REPAIR:
- Treat bundle `binding_status` and `proof_level` as untrusted declarations, not canonical persisted truth.
- In `new_record()`, always derive the persisted proof with `binding_proof_of(protocol, authority=authority, registry=registry)`.
- Remove the direct-return path from `binding_status_of()` for caller-supplied protocol dictionaries; if an already-verified session record needs a fast path, represent that provenance explicitly rather than overloading an untrusted field.
- Keep raw claimed values only under clearly non-authoritative diagnostic fields if needed.
- Reconcile the contradictory binding tests: fixtures must not demand `BOUND/RELEASE_REGISTRY` without a configured authority containing matching historical bytes.
VERIFY:
1. Forged inbox bundle with `binding_status=BOUND`, `proof_level=EXACT_COMMIT`, no authority -> persisted UNKNOWN/PARTIAL, never BOUND.
2. Version+digest without release authority -> PARTIAL.
3. Matching operator release authority -> BOUND/RELEASE_REGISTRY.
4. Matching snapshot authority -> BOUND/CAPTURED_FINGERPRINT.
5. `test_declared_status_cannot_self_promote` PASS.
6. Updated inbox binding test proves authority-derived persistence rather than bundle-declared persistence.
7. Full canonical suite PASS.

[P0] [CORE-002] tools/saipal_engine/{paths.py::HomeLock,hardening.py::SessionLease,enqueue.py::AuditInboxLock}
EVIDENCE:
- All three lock implementations put a random `owner` token into the file but do not retain that token on the lock instance.
- `HomeLock.release()` lines 377-382, `SessionLease.release()` lines 185-190 and `AuditInboxLock.release()` lines 238-245 unconditionally unlink the pathname whenever their local `_held` flag is true; none verifies that the current on-disk lock is still theirs.
- `HomeLock._take_over_if_stale()` lines 361-374 and AuditInboxLock equivalent classify malformed/empty payloads as stale immediately. This includes the create-before-payload interval after `O_EXCL` creates a zero-byte file and before the owner writes JSON.
- TTL expiry can also permit takeover even though the previous instance can subsequently call `release()`.
- Deterministic reproduction with `ttl=0`:
  HomeLock: first acquire=True, second acquire=True, then first.release() -> successor lock no longer exists.
  SessionLease: same result.
  AuditInboxLock: same result.
DEFECT:
Lock ownership is pathname-based rather than ownership-fenced. An old owner can delete a successor's lock, and a second process can steal the zero-byte create-before-write state. HomeLock failure permits overlapping whole-home writers, making session/findings/state last-writer-wins corruption possible; AuditInboxLock exposes audit allocation/ledger races; SessionLease permits duplicate session analysis.
REPAIR:
- Generate one immutable owner token in each lock instance and persist that exact token.
- On release/refresh, reread the lock and mutate/unlink only when the on-disk token equals the instance token.
- Treat newly created/empty/partially written lock files as BUSY for a bounded grace interval, not immediately stale.
- For same-host locks, do not steal a lock owned by a demonstrably live PID merely because wall-clock TTL elapsed; use TTL for dead/unverifiable owners or add an explicit heartbeat/expiry ownership protocol.
- Give all three lock classes identical fencing semantics through one shared helper where practical.
VERIFY:
1. Old owner cannot unlink successor after takeover.
2. Empty create-before-payload lock cannot be stolen immediately.
3. Live same-host PID remains protected across TTL boundary according to the chosen explicit policy.
4. Dead owner is recoverable.
5. Concurrent HomeLock and audit-enqueue stress tests produce one active owner and monotonic durable state.
6. Existing lock tests plus new ownership-token red controls PASS.

[P1] [CORE-003] tools/saipal_engine/pipeline.py::analyze_sessions event budget/checkpoint semantics
EVIDENCE:
- Lines 290-299 call `_candidates_for_session()`, and when no candidate is returned they `continue` before `events_analyzed` and telemetry are incremented at lines 318/322-324.
- Therefore event-budget consumption depends on whether a defect was found, not on how much evidence was analyzed.
- Deterministic reproduction: HOT fixture had episodes `(1,1)` and `(2,4)`; with `Budget(max_events=1)` and `_candidates_for_session` forced to return `[]`, the pipeline reported:
  `sessions_analyzed=1`, `events_analyzed=0`, `last_analyzed_seq=4`, `episodes_exhausted=True`.
- Lines 319-321 set `budget_hit=True` only inside the episode loop; there is no outer-session termination immediately afterward, so later records can still be entered/acquired/checkpointed after the global budget has been exhausted.
DEFECT:
`max_events` is not a real safety bound. Clean/no-drift evidence can consume and permanently watermark arbitrarily more events than the configured budget while reporting zero usage. The resulting checkpoint falsely claims exhaustion and defeats bounded-cycle guarantees.
REPAIR:
- Compute/debit episode span immediately when an episode is actually processed, before candidate-dependent branches.
- Check the prospective span against remaining event budget before beginning the episode; do not process an episode that would violate the declared bound.
- Update telemetry on every processed episode, including zero-candidate episodes.
- Once any global event/time/candidate/audit budget is hit, exit the outer session loop and preserve the exact resumable session/episode position.
- Set `episodes_exhausted` from actual episode completion, not from a global `budget_hit` flag shared across records.
VERIFY:
1. `max_events=1` on the reproduced two-episode fixture never advances beyond the permitted episode/span.
2. Zero-candidate episodes debit events exactly once.
3. Budget hit leaves the next episode pending and resumes it next cycle.
4. Later sessions are untouched after global budget exhaustion.
5. Telemetry count equals analyzed evidence span.

[P1] [CORE-004] tools/saipal_engine/inbox.py::import_inbox / tools/saipal_engine/sessions.py::find_session,already_imported
EVIDENCE:
- PAL-SESSION-01 states: duplicate digest does not create a second session; changed digest creates a new generation.
- `find_session()` lines 155-159 returns only the first matching generation.
- `already_imported()` lines 173-177 checks imports only inside that one record.
- `import_inbox()` lines 82-97 performs dedupe against that first record before allocating a new COLD generation.
- Deterministic sequence:
  import `conformant-cold.json` -> generation 1;
  import `cold-changed-v2.json` -> generation 2;
  import the exact same `cold-changed-v2.json` again -> generation 3.
  Resulting generations contained identical bundle digests for generations 2 and 3.
DEFECT:
COLD identity dedup is only first-generation-aware. Re-importing any digest first introduced in generation 2+ creates unlimited duplicate generations, corrupting the session identity history and defeating idempotent intake.
REPAIR:
- Add `records_for_session(index, session_id)` and perform digest dedupe across every generation before generation allocation.
- Add a separate `latest_record_for_session()` where a single mutable/latest record is actually required instead of relying on first-match `find_session()`.
- Preserve monotonic generation allocation only for genuinely novel COLD content.
VERIFY:
1. V1 -> V2 -> V2 remains exactly generations `[1,2]`.
2. V1/V2 alternated for 5+ cycles still produces two unique generations.
3. Same bundle under a different filename dedupes.
4. Existing HOT append/conflict behavior remains unchanged.

[P1] [CORE-005] tools/saipal_engine/pipeline.py::analyze_sessions / tools/saipal_engine/hardening.py::SessionLease
EVIDENCE:
- `analyze_sessions()` lines 275-280 acquires a SessionLease and executes `continue` when its bundle path is missing without releasing the lease.
- Only bundle `PalError` has an explicit release at lines 281-285; the remainder of the session body is not protected by `try/finally`, so exceptions from analysis, qualification, audit emission, recurrence, etc. can also strand the lease.
- Deterministic reproduction: import a normal COLD session, delete its inbox bundle, call `analyze_sessions()`. Result reports 0 sessions analyzed but `lease-golden-cold-001.json` remains; a new `SessionLease.acquire()` returns False.
- `SessionLease.__init__` line 114 interpolates the raw `session_id` directly into a filesystem path while bundle validation requires only a non-empty string. Separator-bearing provider/generic IDs therefore have filesystem semantics instead of opaque identity semantics.
DEFECT:
Normal error/missing-input paths can permanently wedge a session until TTL/stale recovery, and raw session identifiers are incorrectly used as path components. The same resource should be opaque identity, not a filename grammar.
REPAIR:
- Make SessionLease a context manager or wrap every successful acquire in `try/finally: lease.release()`.
- Hash the session identifier into a filename-safe deterministic key, e.g. SHA-256(session_id UTF-8), while retaining the real ID inside the payload for diagnostics.
- Combine with CORE-002 ownership fencing so release cannot remove another owner's lease.
VERIFY:
1. Missing bundle leaves no held lease.
2. Malformed bundle leaves no held lease.
3. Exception injected from candidate analysis/audit emission leaves no held lease.
4. IDs containing `/`, `\`, `:`, Unicode and long text acquire safely without escaping/nesting the lock directory.
5. Normal contention semantics remain intact.

[P1] [CORE-006] tools/saipal_engine/enqueue.py::enqueue_audit crash consistency
EVIDENCE:
- `allocate_audit_id()` lines 103-127 advances the counter ledger before audit completion.
- `enqueue_audit()` then writes `N.md` at lines 313-333 and only afterward appends the finding/content receipt to `entries.json` at lines 335-345.
- Idempotent retry checks only `entries.json` at lines 304-311.
- Fault reproduction by raising during `_write_ledger` after file creation:
  first call left `audit/staging/1.md` and no entries receipt;
  retry returned audit 2 and left both `1.md` and `2.md` containing the same audit body.
DEFECT:
The documented exactly-once enqueue is not crash-consistent. A crash between immutable file creation and receipt recording creates an unowned orphan; retry cannot identify it and emits a duplicate audit under a new number.
REPAIR:
- Persist an operation intention containing operation_id, finding_id, body digest and allocated number before creating the audit file.
- On retry/startup, reconcile incomplete intentions:
  if the intended file exists with the expected digest, finalize the entries receipt instead of allocating another number;
  if absent, safely continue the same operation;
  if digest conflicts, fail closed.
- Clear/mark the operation complete only after file digest verification and durable receipt commit.
VERIFY:
Fault-inject process failure after each durable stage: id allocation, intention write, audit write, audit fsync/digest verify, entries commit. Every retry must converge to one numbered audit and one receipt with the same digest.

[P1] [CORE-007] tools/saipal_engine/pipeline.py::emit_audit external sink retry lifecycle
EVIDENCE:
- In `PUBLISH_ENABLED`, sink failure is caught at lines 423-431 and the audit is staged locally.
- Line 440 then unconditionally advances the finding to `EMITTED`, regardless of whether external publication succeeded.
- `SAIPAL_OPERATIONAL_HANDOFF.md` states a sink failure keeps the finding staged and retries on the next cycle.
- Deterministic end-to-end reproduction:
  remove configured sink audit directory;
  first `continue` -> local staged audit created and finding state becomes `EMITTED`;
  restore valid sink;
  second `continue` -> `audits_emitted=0`, sink remains empty, finding stays `EMITTED`.
DEFECT:
A transient maintainer-sink outage permanently loses delivery. The local fallback survives, but no durable retry state survives semantically; the system calls the finding emitted and never republishes it.
REPAIR:
- Persist external publication state separately from local audit generation, e.g. PENDING/PUBLISHED with audit number/digest/target.
- Under `PUBLISH_ENABLED`, a sink failure must leave a durable retryable publication record.
- At cycle start, retry pending staged publications idempotently through `sink.lookup_receipt/publish/verify` before declaring them externally delivered.
- Never allocate a new audit for a retry; reuse the existing number/body digest.
VERIFY:
1. Vanished sink -> exactly one local staged audit and durable pending publication.
2. Restore sink -> next cycle publishes that exact audit.
3. Further cycles create no duplicates.
4. Finding/audit status clearly distinguishes local staging from verified external publication.

[P1] [CORE-008] tools/saipal_engine/log.py::last_seq
EVIDENCE:
- `last_seq()` reads only the final `TAIL_WINDOW=65536` bytes at lines 59-86.
- If that entire window contains malformed/torn data, it returns 0 even when valid durable records exist immediately before the window.
- Deterministic reproduction created valid `seq=41` followed by more than `TAIL_WINDOW` malformed bytes:
  `last_seq_before = 0`;
  subsequent `append_event()` wrote `seq=1`;
  `read_events()` then returned valid sequence values `[41, 1]`.
DEFECT:
A sufficiently long torn/corrupt tail resets append numbering, violating monotonic append-only evidence identity. Any consumer using sequence order can see later events as older than historical events.
REPAIR:
- Scan backwards in bounded chunks until a valid JSON event with integer `seq` is found or BOF is reached.
- Correctly handle records split across chunk boundaries.
- Do not silently return 0 on generic I/O errors that mean "unknown" rather than genuinely empty/missing log; fail closed where appropriate.
VERIFY:
1. Valid event + >64 KiB malformed tail -> next sequence is previous+1.
2. Multi-chunk malformed tail works.
3. Torn final partial JSON works.
4. Empty/missing file still yields first sequence 1.
5. Existing append-only/malformed-line tests remain green.

[P1] [CORE-009] tools/saipal_engine/closedloop.py::import_maintainer_disposition
EVIDENCE:
- `import_maintainer_disposition()` lines 132-139 mutates the existing link's disposition/fix/receipt/work/closed fields in place and saves it.
- No revision/history list exists.
- Deterministic reproduction:
  audit 1 -> `REJECTED_FINDING`, receipt R1;
  later audit 1 -> `ENGINE_FIX`, receipt R2, fix 1.2.3;
  persisted link contains only ENGINE_FIX/R2 and `history` is absent. The earlier maintainer decision is destroyed.
- This contradicts the forensic/closed-loop intent and existing test commentary that rejected findings calibrate "without rewriting history".
DEFECT:
Maintainer disposition evidence is not append-only. Corrections, supersession or later resolution erase the prior verdict, making historical calibration/provenance dependent on the last write rather than the actual decision sequence.
REPAIR:
- Store immutable disposition revisions under a `history` array containing disposition, receipt_id, work_id, fix_version and closed_at.
- Keep top-level fields only as a derived/latest projection if compatibility requires them.
- Make receipt/revision identity idempotent: replaying the same receipt performs zero mutation; a genuinely new revision appends.
- Migrate legacy single-value links by treating the existing value as revision 1.
VERIFY:
1. Two different dispositions preserve both chronological revisions.
2. Replaying receipt R1 does not append a duplicate.
3. Calibration can inspect complete history.
4. Legacy links migrate/read safely.
5. Audit file remains immutable.

[P1] [CORE-010] tools/saipal.py::_continue_cycle,cmd_trigger
EVIDENCE:
- `cmd_trigger()` lines 589-603 creates `.saipal/trigger.json` whenever no pending file currently exists.
- `_continue_cycle()` does not atomically claim a trigger at cycle start.
- Instead, lines 165-167 unconditionally delete whatever `trigger.json` exists near the end of the cycle.
- Therefore a new trigger created after the current cycle has already performed source dispatch/intake/analysis but before line 165 is indistinguishable from the old trigger and is deleted.
DEFECT:
A trigger arriving during an active cycle can be acknowledged as "run scheduled" and then silently discarded without causing the requested subsequent cycle. This is a classic lost-wakeup race in unattended operation.
REPAIR:
- Atomically claim the trigger present at cycle start, preferably by rename to a cycle-specific claimed file.
- Process/delete only that claimed token.
- Any `trigger.json` created while the cycle is running must remain pending for the next cycle.
- Preserve coalescing semantics for multiple triggers targeting the same future cycle.
VERIFY:
1. Trigger before cycle -> consumed by that cycle.
2. Trigger injected after claim but before cycle completion -> survives and causes a second cycle.
3. Multiple mid-cycle triggers coalesce to one pending next cycle.
4. Crash after claim has deterministic recovery and does not permanently lose the trigger.

CORE_DONE_WHEN: CORE-001..CORE-010 have regression tests; binding truth is authority-derived; all three lock types are ownership-fenced; analysis budgets are candidate-independent and resumable; COLD digest identity is global across generations; every SessionLease is exception-safe and filename-safe; enqueue fault injection converges exactly once; failed PUBLISH_ENABLED delivery retries the same staged audit; LOG sequence remains monotonic beyond a 64KiB torn tail; disposition revisions are append-only; mid-cycle triggers cannot be lost; targeted red controls pass; validator remains 270/270 or higher; compileall passes; and the complete `python -B tools/test_runner.py` finishes green.

---
## 02 — AUDIT SECOND WAVE
PROJECT_NAME: SAIPAL
DATE_TIME: 2026-09-03T15:41+03:00
CAMPAIGN_PROFILE: quick3
CAMPAIGN_PROFILE_VERSION: 1.0.0
CAMPAIGN_RUN_ID: acb-mtlikhfw-fe84a78fc5a84a868426
CAMPAIGN_MANIFEST_SHA256: 97d9d053e37e6597a8559008911f84b28f799f326a97c69ebc5b304690defa72
WAVE_ID: second
WAVE_INDEX: 2
WAVE_COUNT: 3
WAVE: AUDIT SECOND WAVE
TARGET: _SAIPAL_03.09.26-T15-41-02.zip -> extracted SAIPAL project root
BASELINE: VERSION=0.3.0; archive_sha256=85244f6d2ee5462edad804d8c90c8072098a19acfeb458c6fcbd8c90c0b83fb6
PREVIOUS_WAVE_SHA256: NONE
GIT_CONTEXT: ABSENT - supplied archive contains no .git metadata; branch/commit identity cannot be asserted
SAIPEN_CONTEXT: PRESENT - .saipen STATE is BUILD/T-055; BOARD has pending T-061..T-067. T-067 already mentions a dispatcher cursor as performance work, but W2-001 below materially re-diagnoses it as deterministic correctness/starvation and false-drain behavior.
AUDIT_SCOPE: startup/continue/drain lifecycle; source dispatcher fairness/retry/COLD mutation handling; setup transaction boundary; disposition batch import; recurrence corruption and negative evidence; submission crash/idempotency boundary; closed-loop audit linkage recovery; targeted tests and validator
TEST_STATUS: TEST_PARTIAL
TEST_LIMITATION: full `python -B tools/test_runner.py --json` exceeded the 120s execution ceiling and was terminated; full-suite result unavailable
VERIFIED_INSTEAD: `python -B tools/validate.py` PASS (270 checks); `PYTHONPATH=tools python -B -m unittest -v test_wave_next1.SourceDispatcher test_wave_g_closed_loop.LinkFailureResilience test_wave_submit.AcceptedSubmission.test_submission_is_idempotent_per_unit_verdict_and_reasoning test_wave_spread test_wave_disposition` PASS (46 tests); deterministic temporary-home reproductions verified dispatcher starvation/false drain, rejection-budget bypass, mutated-COLD suppression, partial setup mutation/source-ID collision, partial disposition import/bool acceptance, recurrence overwrite, unreachable clean-model comparison, submission receipt crash duplication, and non-retried closed-loop linkage
STATUS: SECOND_WAVE: COMPLETE
TICKETS: 8
HANDOFF: IMPLEMENTATION_AGENT
COVERAGE_INSPECTED: tools/saipal.py; tools/saipal_engine/{dispatcher,sources,config,state,home,paths,hardening,bundle,inbox,sessions,pipeline,recurrence,closedloop,submit,findings,carrier}.py; provider adapter dispatch contracts; saipal/SESSIONS.md; .saipen/{STATE,BOARD}.md; tools/test_wave_{next1,g_closed_loop,submit,spread,disposition,i_hardening,e_audit}.py; tools/pal_test_support.py; validator
COVERAGE_DEFERRED: exhaustive provider-specific SQLite snapshot races; exhaustive cancellation during every atomic-write stage; Windows-specific filesystem semantics; performance/scaling analysis reserved for wave 3; full canonical test-suite completion
CROSS_WAVE_REFERENCES: CORE-004 is related but distinct from W2-002: CORE-004 covers duplicate generation creation after intake, while W2-002 prevents changed COLD evidence from reaching intake at all. CORE-006 is related but distinct from W2-007: CORE-006 covers audit enqueue crash consistency, while W2-007 covers the outer analyst submission transaction. CORE-007 is related but distinct from W2-008: CORE-007 covers external sink publication retry, while W2-008 covers durable finding-to-audit linkage retry. CORE-009 must be reconciled with W2-004 so disposition history and batch atomicity are implemented together.
RESIDUAL_UNCERTAINTY: no Git revision was supplied; full canonical runner did not complete; crash behavior was fault-injected at Python persistence boundaries rather than tested with process/power termination; no external services or infrastructure were contacted

[P0] [W2-001] tools/saipal_engine/dispatcher.py::dispatch_sources / tools/saipal.py::_continue_cycle,cmd_continue
EVIDENCE:
- `dispatch_sources()` lines 49-99 always begins each source at candidate zero and has no persisted cursor/watermark.
- `count` increases only on imported or unchanged candidates (lines 65, 92, 96). Rejected candidates at lines 97-98 do not consume `max_per_source`.
- Candidates after the fixed prefix are merely counted as `pending` at lines 51-53; nothing causes the next cycle to begin with that tail.
- Deterministic 55-file generic-source reproduction with `max_per_source=50`:
  cycle 1 -> inbox=50, pending=5;
  cycle 2 -> inbox=50, processed=50, pending=5 again.
  The last five candidates are never reached.
- Deterministic rejection reproduction with `max_per_source=10` and 73 candidates whose normalization always fails produced `rejected=73`, `pending=0`, `processed=0`; the advertised per-source bound therefore does not bound attempted work.
- `_continue_cycle()` line 187 defines idle solely as absence of inbox imports/conflicts and ignores `dispatch.pending`.
- Real CLI reproduction with the same 55-source backlog:
  `continue --drain` returned exit 0, `drain_outcome=idle`, `drain_reason="backlog drained"` after two cycles.
  Cycle summaries were `(pending=5, idle=False, sessions_indexed=50)` then `(pending=5, idle=True, sessions_indexed=0)`.
  Only 50 sessions existed afterward.
DEFECT:
The dispatcher has neither fair progress state nor a true attempt budget. A stable prefix of already-seen candidates permanently starves the tail, malformed candidates bypass the cycle bound entirely, and `--drain` can explicitly report "backlog drained" while known dispatcher work remains. This breaks unattended repeatability and turns the existing T-067 cursor concern from performance drift into functional data-intake loss.
REPAIR:
- Make `max_per_source` bound candidate attempts, including rejected/unchanged candidates.
- Persist deterministic per-source progress/cursor state sufficient to resume after the last attempted candidate rather than restarting at the sorted prefix.
- Define cursor invalidation/reconciliation when source membership changes so new earlier-sorted items are not missed.
- Include dispatcher pending state in `_continue_cycle().idle`.
- `--drain` may return idle only when dispatcher pending=0, intake has no work/conflicts, and analysis has no resumable work.
- Keep per-source isolation: one bad source/candidate must not block progress through other sources.
VERIFY:
1. 55 stable candidates with limit 50 -> first cycle handles 50, second handles remaining 5, third is genuinely idle.
2. 73 rejected candidates with limit 10 -> exactly 10 attempts in one cycle and remaining work is reported pending.
3. Mixed unchanged/rejected/new candidates advance fairly without starvation.
4. `continue --drain` never says "backlog drained" while any dispatcher reports pending.
5. Restart between cycles resumes the same durable cursor correctly.
6. Existing SourceDispatcher tests remain green.

[P0] [W2-002] tools/saipal_engine/submit.py::submit_candidate
EVIDENCE:
- Receipt idempotency is checked first at lines 250-272.
- For a new submission, durable side effects occur before the receipt:
  DRIFT enters `_absorb_drift()` at lines 280-283;
  NO_DRIFT writes recurrence at line 285;
  semantic state is advanced at line 287 and the session index is durably saved at line 288.
- Only afterward is the idempotency receipt appended and persisted at lines 290-303.
- Fault injection replacing `_save_receipts()` with an exception after the earlier writes produced:
  first submission -> RuntimeError, no receipt file, session semantic `no_drift=1`, recurrence already advanced;
  retry of the exact same candidate -> `duplicate=False`, session semantic `no_drift=2`, recurrence advanced a second time.
- In the reproduction recurrence occurrence count moved from the already-existing baseline to 2 after the failed submission and to 3 after the retry.
- Existing `test_submission_is_idempotent_per_unit_verdict_and_reasoning` passes because it covers only a fully completed first submission followed by a retry; it does not cover a crash before receipt persistence.
DEFECT:
The receipt is being used as the idempotency authority but is committed after the effects it is meant to protect. A crash/error in that interval leaves durable state with no durable operation identity, so the exact retry is treated as novel and double-applies semantic/recurrence state. The same ordering also exposes DRIFT submissions to orphaned finding/audit side effects before their submission receipt exists. This is durable state corruption.
REPAIR:
- Introduce a durable submission operation/intention keyed by the current receipt id before applying semantic, recurrence, finding, or audit mutations.
- Make every subsequent stage replayable from that operation and persist completion stage/digests.
- On retry, reconcile an incomplete operation and finish it rather than applying effects again.
- Commit the final receipt only after all required effects are verified; mark the operation complete afterward.
- For NO_DRIFT, recurrence and semantic advancement must be exactly once.
- For DRIFT, finding merge/audit operation ids must be reused during recovery rather than regenerated.
VERIFY:
1. Fault after operation creation but before side effects -> retry completes once.
2. Fault after recurrence write -> retry does not add another occurrence.
3. Fault after session-index semantic write -> retry does not increment counters again.
4. Fault during DRIFT finding creation -> retry converges to one finding.
5. Fault after audit enqueue but before submission receipt -> retry reuses the same audit.
6. Fully completed duplicate submissions remain idempotent as today.

[P1] [W2-003] tools/saipal_engine/dispatcher.py::dispatch_sources COLD known-session fast path
EVIDENCE:
- PAL-SESSION-01 / PAL-SESSION-02 in `saipal/SESSIONS.md` lines 27-29 and 73-74 require: unchanged COLD digest -> skip; changed COLD digest -> new generation.
- `dispatch_sources()` lines 54-68 checks only whether the candidate-declared `session_id` is already indexed.
- If its existing inbox bundle is COLD, it returns `"already indexed, cold"` at lines 63-66 before calling `adapter.normalize()` or computing current source identity/digest at lines 69-85.
- Deterministic adapter reproduction:
  first source version `v1` was dispatched/imported;
  the same native session was then changed to `v2` with a different valid bundle digest;
  second dispatch reported `"already indexed, cold"`;
  adapter normalize call count remained 1 total;
  inbox evidence remained tagged `v1`;
  `v1.session_sha256 != v2.session_sha256`.
DEFECT:
The dispatcher treats "known COLD session id" as equivalent to "same immutable artifact". That is exactly the invariant the protocol says must be decided by digest. Changed completed evidence is silently suppressed before it can reach the generation logic. Fixing CORE-004 alone cannot repair this because the changed artifact never reaches `import_inbox()`.
REPAIR:
- Never classify a COLD candidate as unchanged solely from session id.
- Obtain current source identity/content digest before the fast-path decision.
- Compare the current artifact against every known generation/digest for that source identity.
- Equal digest -> skip safely.
- Novel digest -> normalize, validate, stage it so intake creates the next generation.
- If a provider can expose a trustworthy cheap content identity, use it as the pre-normalization optimization; otherwise normalize rather than guessing.
VERIFY:
1. COLD V1 -> unchanged V1 skips.
2. COLD V1 -> changed V2 reaches intake and creates generation 2.
3. V1 -> V2 -> unchanged V2 does not create generation 3 after CORE-004 is fixed.
4. Provider candidate session id alone can never suppress a changed digest.
5. Existing known-COLD fast-path tests are rewritten to assert digest-aware behavior.

[P1] [W2-004] tools/saipal.py::cmd_setup
EVIDENCE:
- `cmd_setup()` reads/mutates the source registry at lines 395-405 and immediately calls `save_sources()` at line 406.
- Validation and persistence of the requested publication configuration happen later at lines 408-449.
- Reproduction:
  `setup --source <valid-dir> --kind generic --mode PUBLISH_ENABLED`
  without `--shadow-reviewed` or a sink raised
  `refusing invalid config: PUBLISH_ENABLED requires shadow_reviewed; PUBLISH_ENABLED requires configured sink.root`,
  but `sources.json` had already been durably changed to contain the new source.
- The same source-edit block removes an existing path then assigns the replacement id using `src{len(sources)+1}` at lines 400-405.
- Reproduction with existing `src1=A, src2=B`, then setup of path A again:
  A is removed, list length becomes 1, replacement is assigned `src2`, and `save_sources()` fails with `duplicate source id 'src2'`.
DEFECT:
One `setup` invocation is not a coherent configuration transaction. A command that reports failure can still alter an earlier subsystem, and the length-derived identifier makes ordinary reconfiguration of an existing source fail once ids have gaps/collisions. This creates error-state drift precisely at the operator boundary intended to establish a reproducible runtime.
REPAIR:
- Parse the entire requested setup operation into prospective source/config/state payloads without writing.
- Preserve the existing source id when updating the same source path; for genuinely new sources allocate the first unused stable id rather than deriving it from list length.
- Validate every prospective payload and every referenced directory before the first durable mutation.
- Commit all affected setup files under the home writer lock with a small recoverable setup journal or equivalent rollback-safe transaction so a process failure between files cannot leave ambiguous half-configuration.
- On any validation failure, tree digest must remain unchanged.
VERIFY:
1. Invalid PUBLISH_ENABLED setup writes nothing.
2. Updating source A in `[src1=A, src2=B]` preserves src1 and succeeds.
3. Delete/gap patterns such as src1/src3 allocate a non-colliding id.
4. Combined source+sink+mode+authority setup commits all requested changes or none.
5. Fault injection between each file commit recovers deterministically on next startup.

[P1] [W2-005] tools/saipal_engine/closedloop.py::import_disposition_file
EVIDENCE:
- `import_disposition_file()` lines 143-164 validates and applies rows one at a time.
- Each successful row calls `import_maintainer_disposition()`, which mutates the link and immediately saves the whole ledger at lines 132-139.
- Reproduction with two rows:
  row 1 = audit 1 / `NO_CHANGE` / receipt R1;
  row 2 = invalid disposition `BOGUS`.
  Import raised on row 2, but the durable audit-1 link had already changed to `NO_CHANGE`, receipt `R1`.
- The row shape check at line 154 uses `isinstance(audit_number, int)` without excluding bool.
- JSON `{"audit_number": true, "disposition": "ENGINE_FIX"}` was accepted and resolved to audit number 1 because Python bool is an int subclass, mutating audit 1.
DEFECT:
Disposition-file import is neither atomic nor type-strict. A failed batch can partially alter maintainer provenance, and a JSON boolean can be interpreted as a real audit identifier. This becomes even more dangerous when CORE-009 adds immutable disposition revisions, because a naïve retry after a partial failure could append duplicate historical decisions.
REPAIR:
- Parse and validate the complete file before mutation.
- Reject bool explicitly for every integer identifier.
- Validate disposition enum, audit existence, receipt/work/fix field types, and intra-file duplicate semantics across all rows.
- Apply the validated batch to an in-memory copy of the link ledger/history and persist once.
- Make receipt identity idempotent so replay of the same complete batch is a no-op.
- Integrate this with CORE-009's append-only revision representation rather than adding a second disposition store.
VERIFY:
1. Any invalid row leaves the entire link ledger byte-for-byte unchanged.
2. `audit_number=true/false` is rejected.
3. Valid multi-row batch commits all rows once.
4. Replaying the same receipt batch adds no duplicate revisions.
5. One row referencing a missing audit causes zero mutations to all other rows.

[P1] [W2-006] tools/saipal_engine/recurrence.py::load_recurrence,record_occurrence,record_negative
EVIDENCE:
- `load_recurrence()` lines 82-91 converts both unreadable JSON and structurally invalid recurrence data into `empty_recurrence()` instead of distinguishing corruption from absence.
- Both writers then accept that empty value and overwrite `recurrence.json` via `save_recurrence()` at lines 132-148.
- Deterministic reproduction wrote a parseable ledger with `schema_version=999` and historical sentinel finding `"old"`, then called `record_negative()`.
- The call succeeded; resulting recurrence had schema 1, `"old"` was gone, and only the new `CONFORMANT` bucket remained.
- `_record_recurrence()` in pipeline lines 371-399 intends recurrence failure to be non-fatal and logged, but corruption never reaches its exception handler because `load_recurrence()` silently converts it into a valid empty ledger.
DEFECT:
Corrupt/incompatible recurrence state fails open as "new empty database" and is overwritten on the next analyzed session. Historical recurrence evidence is thereby silently destroyed. Non-fatal analytics handling does not require destructive recovery; the current implementation confuses "skip this update" with "replace history".
REPAIR:
- Make recurrence loading return explicit `absent | ok | unrecoverable` status like session/findings/config stores.
- Only absent state may initialize an empty ledger.
- Unreadable, invalid-schema, or malformed state must refuse recurrence mutation and preserve the original bytes.
- Pipeline may continue analysis, but must log `recurrence_record_failure` and leave recurrence untouched.
- If schema migration is supported later, make it explicit/versioned and preserve source evidence.
VERIFY:
1. Missing recurrence initializes normally.
2. Malformed JSON remains byte-for-byte unchanged after analysis; failure event is logged.
3. Unsupported schema remains unchanged and is reported.
4. Valid recurrence continues appending.
5. No clean/drift session can silently reset historical recurrence.

[P1] [W2-007] tools/saipal_engine/recurrence.py::record_negative,_breakdown,spread / tools/saipal_engine/pipeline.py::_record_recurrence
EVIDENCE:
- Runtime conformant sessions are recorded by pipeline lines 378-381 through `record_negative()`.
- `record_negative()` lines 140-148 always stores them under a synthetic finding whose `drift_class` is exactly `"CONFORMANT"`.
- `spread(data, drift_class)` obtains its model/project comparison through `_breakdown()`.
- `_breakdown()` lines 151-167 discards every recurrence bucket whose finding `drift_class` is not the requested drift class.
- Therefore clean evidence written by the real runtime is excluded from every actual drift class such as `COMMAND_ROUTE_DRIFT`.
- Deterministic live-writer reproduction:
  record drifting `gpt` occurrence for `COMMAND_ROUTE_DRIFT`;
  record conformant `claude` via `record_negative`;
  `spread(..., "COMMAND_ROUTE_DRIFT")` returned
  `drifting_models=["gpt"]`,
  `clean_models=[]`,
  `contradicted_by_a_clean_model=False`,
  guidance `"no comparison available"`.
- Targeted existing spread tests all pass. Inspection shows the decisive `test_a_clean_model_beside_a_drifting_one_is_the_decisive_case` fabricates comparison data directly, while the live-ledger test exercises multiple drifting models; neither proves that runtime negative evidence reaches the target drift comparison.
DEFECT:
The advertised "another model complied under the same rule" safeguard is unreachable from normal recurrence writers. Runtime negative evidence is stored in a separate taxonomy bucket that `spread()` intentionally filters out, so the engine can never obtain its strongest evidence for model-specific noncompliance from actual clean sessions.
REPAIR:
- Record conformant evidence against the same comparison scope used by drift evidence: applicable rule ids and/or drift-class comparison keys actually exercised by that session.
- Do not infer that a clean session disproves every drift class globally; only associate negative evidence with rules/surfaces the session demonstrably covered.
- Build spread from those shared comparison keys so drifting and clean model observations meet in the same denominator.
- Preserve the generic CONFORMANT summary only as an auxiliary aggregate if still useful.
VERIFY:
1. A clean model exercising PAL-CMD-01 plus a drifting model on the corresponding drift class yields `clean_models=["claude"]` and `contradicted_by_a_clean_model=True`.
2. A clean session that did not exercise the relevant rule is not counted.
3. Two drifting models remain CROSS_MODEL.
4. Clean-only evidence remains non-drift and cannot manufacture a finding.
5. Add a true end-to-end test using `_record_recurrence`/CLI rather than a fabricated recurrence ledger.

[P1] [W2-008] tools/saipal_engine/pipeline.py::emit_audit / tools/saipal_engine/closedloop.py::link_finding_audit
EVIDENCE:
- `emit_audit()` persists/enqueues the audit, attaches its metadata to the finding, and advances the finding to `EMITTED` at lines 434-441.
- Durable closed-loop linkage happens only afterward at line 446.
- Any linkage failure is caught at lines 447-460 and reduced to a `closed_loop_link_failure` log event.
- Search of live runtime code finds no second caller/reconciliation path for `link_finding_audit`; `emit_audit()` is the only runtime attempt.
- Existing `LinkFailureResilience` test explicitly proves only that the audit survives and a failure event is logged; it does not prove eventual linkage.
- End-to-end reproduction:
  start with malformed `closed_loop_links.json`;
  ingest a drift bundle;
  first `continue` exited 0, emitted one audit, persisted finding `PAL-0001` as `EMITTED`, and links remained unrecoverable;
  manually repair `closed_loop_links.json` to a valid empty ledger;
  second `continue` exited 0 with `audits_emitted=0`;
  valid link ledger remained empty.
DEFECT:
A transient or repairable link-ledger failure permanently severs finding -> audit provenance. Logging the failure makes it visible but not recoverable: the finding has already crossed the lifecycle gate that would call linkage, so subsequent cycles never reconstruct the missing link. This contradicts the closed-loop ledger's stated durable cold-restart purpose.
REPAIR:
- Persist a retryable link operation keyed by finding id + audit number/operation id before or atomically with advancing the finding to EMITTED.
- On every continue/startup, reconcile pending link operations before declaring the closed-loop surface healthy.
- If the audit exists and digest matches, retry the same link idempotently; never create another audit.
- A malformed link ledger should leave the operation pending and log the reason without discarding provenance intent.
- `link_finding_audit()` itself should dedupe by stable audit/finding identity on retry.
VERIFY:
1. Corrupt link ledger -> audit survives and one pending linkage is durable.
2. Repair ledger -> next cycle creates exactly one finding/audit link automatically.
3. Additional cycles do not duplicate the link.
4. Restart between failure and repair preserves the pending operation.
5. Digest/audit identity conflict fails closed instead of linking the wrong audit.

SECOND_WAVE_DONE_WHEN: W2-001..W2-008 have regression controls; dispatcher progress is durable/fair and drain cannot false-idle; changed COLD evidence reaches generation logic; setup is all-or-nothing with stable non-colliding source ids; disposition batches are prevalidated, atomic, bool-safe and compatible with CORE-009 history; recurrence corruption never destroys prior evidence; real conformant sessions participate in rule-scoped clean-model comparison; candidate submission is crash-idempotent across every durable stage; failed finding-to-audit linkage is durably retried; validator remains >=270/270; targeted suites remain green; and the complete `python -B tools/test_runner.py --json` finishes successfully.

---
## 03 — AUDIT PERFORMANCE
PROJECT_NAME: SAIPAL
DATE_TIME: 2026-09-03T15:41+03:00
CAMPAIGN_PROFILE: quick3
CAMPAIGN_PROFILE_VERSION: 1.0.0
CAMPAIGN_RUN_ID: acb-mtlikhfw-fe84a78fc5a84a868426
CAMPAIGN_MANIFEST_SHA256: 97d9d053e37e6597a8559008911f84b28f799f326a97c69ebc5b304690defa72
WAVE_ID: performance
WAVE_INDEX: 3
WAVE_COUNT: 3
WAVE: AUDIT PERFORMANCE / STABILITY / EFFECTIVENESS
TARGET: /mnt/data/_SAIPAL_03.09.26-T15-41-02.zip -> extracted project root /mnt/data/audit_perf_saipal
BASELINE: VERSION=0.3.0; archive_sha256=85244f6d2ee5462edad804d8c90c8072098a19acfeb458c6fcbd8c90c0b83fb6
PREVIOUS_WAVE_SHA256: NONE
GIT_CONTEXT: ABSENT - supplied archive contains no .git metadata; no branch/commit identity can be asserted
SAIPEN_CONTEXT: PRESENT - .saipen/STATE.md is BUILD/T-055; BOARD T-067 already names a performance sweep covering detector scaling, dispatcher progress, HOT watermarking, inbox cursoring, recurrence batching and index compaction; findings below were independently verified against live implementation
AUDIT_SCOPE: episode/detector analysis scaling; canonical inbox intake; source dispatcher and HOT adapter normalization; OpenCode SQLite adapter; session-index persistence/load amplification; recurrence persistence; status/log memory behavior; current runtime-state size; targeted performance-related tests
TEST_STATUS: TEST_PARTIAL
TEST_LIMITATION: `PYTHONPATH=tools python -B -m unittest -v test_wave_b_episodes test_wave_c_detectors test_wave_h_recurrence test_wave_i_hardening test_wave_next1` exceeded the 120s execution ceiling while entering `test_wave_next1.SinkAbstraction.test_preflight_requires_manifest`; every displayed test before timeout passed, so the complete targeted set was not verified
VERIFIED_INSTEAD: `python -B tools/validate.py` PASS (270 checks); `python -B -m compileall -q tools` PASS; read-only runtime measurements and isolated temporary-home/synthetic benchmarks verified quadratic detector scaling, repeated 36.6MB inbox parsing, unused cheap HOT watermark path, 11.1MB monolithic index amplification, recurrence N-write amplification and unbounded LOG materialization
STATUS: PERFORMANCE: COMPLETE
TICKETS: 6
HANDOFF: IMPLEMENTATION_AGENT
COVERAGE_INSPECTED: tools/saipal.py::_continue_cycle,_load_read_only,cmd_status; tools/saipal_engine/{detectors,analyst,pipeline,inbox,bundle,dispatcher,sessions,episodes,carrier,coverage,recurrence,log,paths}.py; tools/saipal_engine/adapters/{opencode,generic,claude,codex,gemini,termisai}.py; .saipal runtime state; .saipen STATE/BOARD; relevant Wave B/C/H/I/next1 tests
COVERAGE_DEFERRED: exhaustive real-world large-store profiling for every provider adapter; Windows-specific filesystem/cache timing; long-duration soak/RSS profiling; complete test runner; W2-001 dispatcher fairness/cursor correctness is not duplicated as a performance ticket
CROSS_WAVE_REFERENCES: W2-001 already owns the missing persistent dispatcher cursor, attempt budgeting and false-drain correctness; W2-003 owns changed-COLD suppression and must remain intact while optimizing dispatch; CORE-003 owns event-budget correctness; W2-006/W2-007 own recurrence corruption and negative-evidence semantics; optimizations below must preserve those repairs rather than bypass them
RESIDUAL_UNCERTAINTY: absolute benchmark timings are sandbox-specific and should not be treated as Windows timings; scaling ratios and I/O counts follow directly from implementation and reproduced workloads; current LOG is small, so PERF-006 is a demonstrated long-run stability bound rather than a present dominant cost

[P1] [PERF-001] PROVEN BOTTLENECK tools/saipal_engine/detectors.py::_span,detect_all,detect_active_work_preemption,detect_source_closure_false_green / tools/saipal_engine/analyst.py::contrary_evidence_pass
EVIDENCE:
- `_span()` rebuilds a list by scanning every bundle event for every detector invocation on every episode.
- `detect_all()` invokes the configured detector set independently per episode; multiple detectors call `_span()`, while `detect_active_work_preemption()` and `detect_source_closure_false_green()` additionally scan `_events(bundle)` beyond the episode.
- `contrary_evidence_pass()` copies and scans the complete event stream again for each candidate.
- Synthetic no-candidate stream with 10 events per episode, using the real detector registry:
  1,000 events / 100 episodes -> median 0.020502s
  2,000 / 200 -> 0.080888s
  4,000 / 400 -> 0.328856s
  8,000 / 800 -> 1.266508s
- Doubling input repeatedly costs approximately 4x, empirically demonstrating O(N^2)-class growth before historical-rule or finding work.
- Current runtime already contains 49,546 indexed events and 14,785 episodes, so this is on the actual analysis growth path rather than a dead helper.
ISSUE:
Episode analysis repeatedly rediscovering spans from the full bundle makes runtime scale with episodes × events. Long agent sessions therefore become disproportionately expensive exactly where SAIPAL is intended to process long-running evidence. Candidate-producing episodes add further full-stream contrary-evidence passes.
OPTIMIZE:
- Build one ordered event/sequence index per loaded bundle.
- Resolve episode boundaries to event-array offsets once, then pass span views/slices or offset pairs through the detector/analyst pipeline.
- Precompute suffix/prefix facts needed by detectors that intentionally inspect later evidence, such as "terminal event exists after seq X", instead of rescanning the full bundle.
- Restrict contrary-evidence iteration to the semantically legal episode range using the same offsets; preserve its special ability to notice an earlier user override.
- Avoid repeated `list(bundle["events"])` copies in hot detector helpers.
GUARDRAIL:
- Event sequence numbers are ordered but must not be assumed contiguous; use indexed positions/bisect or an explicit seq->offset map.
- `detect_active_work_preemption` and `detect_source_closure_false_green` intentionally inspect evidence after a trigger; an optimization must reproduce that semantics, not blindly clip every detector to the episode.
- Candidate ordering, event_refs, confidence, contrary-evidence text and no-hindsight behavior must remain identical for the same bundle.
VERIFY:
1. Existing Wave B/C detector and analyst fixtures produce structurally identical candidates before/after.
2. Add 1k/2k/4k/8k scaling regression with episodes proportional to events.
3. Doubling the synthetic workload should remain near-linear rather than ~4x; set a tolerant CI bound appropriate to shared runners.
4. Candidate-heavy benchmark separately proves contrary-evidence passes do not restore quadratic growth.
5. Memory stays O(events + episodes), not O(events × episodes).

[P1] [PERF-002] PROVEN BOTTLENECK tools/saipal_engine/inbox.py::import_inbox / tools/saipal_engine/bundle.py::load_bundle_file,validate_bundle,bundle_digest
EVIDENCE:
- Current `.saipal/session_inbox` contains 99 canonical JSON files totaling 36,639,390 bytes.
- `import_inbox()` enumerates every inbox file on every invocation, fully loads/parses/validates it, then performs session lookup even when previously consumed.
- A temporary copy of the current runtime home repeatedly took approximately 0.96-1.00s per ordinary `import_inbox()` call.
- cProfile of one pass: 1,891,643 function calls in 1.344s.
- Top cumulative costs included:
  `load_bundle_file`: 99 calls / 0.869s;
  `parse_bundle` + `validate_bundle`: 99 calls / 0.679s;
  JSON encoding: 201 calls / ~0.500s;
  `bundle_digest`: 198 calls / 0.476s;
  `_validate_events`: 99 calls / 0.438s.
- A bundle carrying `session_sha256` is canonically hashed during validation, and `import_inbox()` immediately calls `bundle_digest()` again, explaining the two digest/canonical-serialization passes per bundle.
- `save_index()` is invoked after the scan even when intake has no useful index mutation.
ISSUE:
The durable inbox is being treated as a fresh batch queue every cycle. Historical canonical evidence is reparsed, revalidated and reserialized indefinitely, so continue cost grows with all evidence ever retained instead of new/changed evidence. The double canonical digest compounds the waste.
OPTIMIZE:
- Add a durable intake receipt/journal for each canonical inbox artifact containing its trusted content identity and last disposition.
- Fast-path artifacts whose exact content identity has already been consumed; fully parse only new or changed bytes.
- Compute the canonical bundle digest once per parsed document and reuse it for validation/import.
- Build session-id/generation digest lookup maps once per intake pass rather than repeatedly scanning the session list.
- Do not rewrite the session index when no index field changed.
- For SAIPAL-generated inbox artifacts, persist the content digest at atomic creation so intake can reuse that authority instead of regenerating canonical JSON.
GUARDRAIL:
- A filename alone, and mtime/size alone, must never prove unchanged content.
- Directly supplied inbox files may be replaced under the same name; the optimization must still detect changed bytes, malformed replacements, COLD generations and HOT prefix conflicts.
- A rejected artifact may be skipped only while its byte identity is unchanged; changing it must trigger validation again.
- Coordinate with CORE-004/W2-003 so performance shortcuts cannot reintroduce duplicate generations or suppress changed COLD evidence.
VERIFY:
1. First pass over N bundles validates/imports them normally.
2. Second unchanged pass performs zero bundle JSON parses/canonical serializations and zero session-index write.
3. Modify one byte-valid bundle under the same filename -> exactly that artifact is reparsed/reconciled.
4. Modify a previously rejected artifact -> it is retried.
5. Digest/duplicate/conflict outcomes remain identical to the unoptimized implementation.
6. Benchmark the current 99-file/36.6MB fixture: unchanged-cycle intake should fall from ~1s to a small bounded metadata/receipt check.

[P1] [PERF-003] PROVEN BOTTLENECK tools/saipal_engine/dispatcher.py::dispatch_sources / tools/saipal_engine/adapters/opencode.py::stable_watermark,normalize,identity,_read_rows
EVIDENCE:
- Every adapter exposes `stable_watermark()`, but repository search finds only the adapter definitions; dispatcher never calls the hook.
- For known HOT candidates, `dispatch_sources()` proceeds through full `adapter.normalize()` every cycle before deciding whether the resulting canonical bundle is unchanged.
- OpenCode `stable_watermark()` performs a cheap SQLite `count(*)` for the session.
- OpenCode `normalize()` loads all message and part rows, JSON-decodes them, rebuilds every canonical event, then calls `identity()`.
- `identity()` queries all parts again and creates one giant `"".join(json.dumps(...))` blob before hashing, causing a second full traversal plus an avoidable full-size intermediate string.
- Synthetic OpenCode SQLite benchmark:
  1,000 parts: watermark 0.000224s, normalize 0.012064s, 53.9x difference
  2,000: 0.000347s vs 0.026580s, 76.5x
  4,000: 0.000486s vs 0.050316s, 103.5x
  8,000: 0.000816s vs 0.112549s, 138.0x
- Current index contains 35 HOT sessions, so repeated normalization is a normal operating path.
ISSUE:
HOT polling cost is proportional to complete session history even when nothing changed. As sessions grow, every continue re-reads and re-normalizes old provider history, while the already-implemented cheap progress hook is unused. OpenCode additionally duplicates the full part scan during identity generation.
OPTIMIZE:
- Define a dispatcher/adaptor probe contract that returns a cheap stable watermark plus sufficient source identity information before normalization.
- Persist the last accepted provider watermark/source fingerprint with the session.
- If the probe proves no change under the provider's append-only contract, skip normalization.
- When growth is detected, add tail normalization from the prior provider cursor/byte offset/row key and merge only the canonical suffix after proving the analyzed prefix still matches.
- For OpenCode, stream the identity hash row-by-row rather than materializing one giant concatenated string, and avoid querying the same unchanged rows twice.
- For JSONL providers, retain byte offset plus validated prefix identity so new lines can be read from the tail.
GUARDRAIL:
- `count(*)` alone is insufficient to prove an OpenCode source unchanged if rows can mutate in place. A fast probe must use provider guarantees or additional identity evidence, with full normalization as the safe fallback.
- Never weaken HOT prefix-conflict detection.
- W2-001 owns dispatcher cursor/fairness and W2-003 owns COLD change detection; share progress state where useful but do not let a HOT optimization become a COLD session-id fast path.
- Reasoning/hidden-content filtering and evidence locator semantics must remain unchanged.
VERIFY:
1. Unchanged HOT session -> no full normalize call.
2. Append one provider event/part -> only the tail is normalized and canonical result equals a fresh full normalize byte-for-byte after digest normalization.
3. Mutate an already accepted prefix -> conflict/full-verification path fires.
4. Restart preserves the provider cursor/watermark.
5. OpenCode 1k/2k/4k/8k polling with no changes remains approximately constant/sublinear rather than proportional to full history.
6. Tail-append cost scales with appended rows, not total rows.

[P1] [PERF-004] PROVEN BOTTLENECK tools/saipal_engine/sessions.py::new_record,load_index,save_index / tools/saipal_engine/inbox.py::_extend_hot / tools/saipal_engine/pipeline.py::analyze_sessions / tools/saipal.py::_continue_cycle,cmd_status
EVIDENCE:
- Current `.saipal/sessions/index.json` is 11,129,891 bytes for only 101 session records.
- It contains 14,785 persisted episode objects, 6,812 mechanical spans and 15,520 copied evidence-reference entries.
- Re-serializing the exact current index with the same pretty-JSON format while removing one duplicated/derived field at a time shows:
  `episodes`: 2,552,251 bytes / 22.93%;
  `mechanical_spans`: 1,449,748 / 13.03%;
  `evidence_refs`: 6,934,001 / 62.30%.
- Removing all three from the same representation leaves 193,891 bytes, a 98.26% reduction. This is a measurement of duplication potential, not an instruction to delete required semantics.
- Repository search finds `mechanical_spans` written during import/HOT extension but no runtime consumer.
- Current `load_index()` median: ~0.0630s; `save_index()` median on a temporary copy: ~0.0896s.
- `_continue_cycle()` loads the index before dispatch, `import_inbox()` loads and saves it internally, then `_continue_cycle()` loads it again before analysis.
- `analyze_sessions()` unconditionally saves findings index, session index and telemetry even when every session is exhausted.
- On the current 101-session baseline, a temporary-home `analyze_sessions()` doing 0 sessions, 0 events and 0 findings still took median ~0.108s and rewrote the identical 11,129,891-byte index.
- `cmd_status()` loads the same session index in `_load_read_only()` and then loads it again for coverage.
- Read-only current-baseline `saipal --json status` measured ~0.76s with process max RSS 153,280KB in this sandbox.
ISSUE:
A small amount of authoritative mutable session state is embedded in a large monolithic document dominated by copied/derived evidence metadata. The whole structure is repeatedly parsed, validated and atomically rewritten even for no-op cycles. This produces fixed CPU, allocation and disk-write amplification that grows with historical evidence, not current work.
OPTIMIZE:
- Immediately stop persisting `mechanical_spans` if parity tests confirm the repository-wide search result that it has no reader.
- Stop unconditional `save_index()` calls; track dirty state and write only when a record actually changes.
- Thread an already-loaded index through dispatch/intake/analysis where ownership permits instead of loading the same 11MB document multiple times in one cycle.
- Refactor the durable session store so the hot mutable registry contains compact authoritative identity/cursor/status fields while large evidence-derived structures are either compact per-session sidecars or rebuilt from the canonical bundle under the exact segmentation-version authority that created them.
- Avoid duplicating full `evidence_ref` payloads in both canonical bundle and session index; use a compact locator index/sidecar where random lookup is required.
- Preserve backward reading and provide an explicit atomic schema migration rather than silently discarding old fields.
GUARDRAIL:
- `episodes` are derived using registry-controlled boundary semantics; never lazily regenerate historical episodes under a newer registry without preserving the original segmentation contract/version.
- Canonical bundles remain forensic evidence and must not be mutated to gain speed.
- HOT prefix digest, semantic cursor, generation, imports, conflict state and binding proof must remain durable.
- Any sidecar used as authority must have explicit schema/digest linkage and atomic-write behavior; otherwise it is cache only and must be rebuildable.
VERIFY:
1. Existing 101-session runtime migrates with identical status, coverage, next-carrier and evidence behavior.
2. No-work analysis performs zero session-index write.
3. One changed session rewrites only the intended compact state/sidecar, not unrelated multi-megabyte evidence.
4. Repeated status/continue results are byte-for-byte or structurally equivalent at the API boundary.
5. Measure index/store footprint on the current baseline; target an order-of-magnitude reduction, with the measured theoretical duplicate-field ceiling documented rather than blindly enforced.
6. Restart/corruption tests prove compact state can still recover or fail closed without losing forensic evidence.

[P2] [PERF-005] PROVEN BOTTLENECK tools/saipal_engine/pipeline.py::_record_recurrence / tools/saipal_engine/recurrence.py::record_occurrence,record_negative,load_recurrence,save_recurrence
EVIDENCE:
- `_record_recurrence()` loops over every session finding and invokes `record_occurrence()` separately.
- Every `record_occurrence()` loads the entire recurrence JSON, mutates one occurrence, validates it, pretty-serializes the entire ledger and atomically replaces the file.
- Synthetic valid recurrence ledger with 5,000 existing occurrences was 1,834,277 bytes.
- Recording 20 findings through the public per-finding path took 0.3944s.
- One `load_recurrence()` + 20 identical `_bump_occurrence()` calls + one `save_recurrence()` took 0.0146s.
- Same logical update therefore showed ~26.9x write-path speedup from batching alone.
- Current recurrence ledger is only 47,581 bytes, so the defect is presently smaller than PERF-001..004 but grows directly with accumulated recurrence history and findings/session.
ISSUE:
Persistence granularity is one full ledger transaction per finding instead of one transaction per analyzed session/submission batch. Multi-finding sessions create O(findings × ledger_size) parse/serialization/write amplification.
OPTIMIZE:
- Add a batch recurrence API that loads once, applies all occurrences in memory, validates once and atomically saves once.
- Make pipeline `_record_recurrence()` submit the complete session finding set through that API.
- Keep single-occurrence API as a wrapper around the batch primitive for callers that genuinely have one update.
- Avoid writing when the batch is empty or idempotently already recorded, once submission idempotency work from W2-002 is integrated.
GUARDRAIL:
- Coordinate with W2-006: malformed/unsupported recurrence data must fail closed and remain byte-for-byte untouched, never be converted to an empty ledger before a batch save.
- Coordinate with W2-007: batching must not alter the rule-scoped clean/drift comparison semantics.
- One batch must remain one atomic durable transition; do not trade repeated atomic writes for a partially written in-place format.
VERIFY:
1. Sequential reference implementation and batch implementation produce equivalent valid recurrence content for the same ordered occurrences.
2. Mock/trace proves one load and one atomic save for N findings.
3. Inject failure before final save -> old ledger remains intact.
4. Duplicate/retry behavior remains exactly once after W2-002 recovery semantics are applied.
5. Re-run 5,000-existing + 20-new benchmark and retain the order-of-magnitude reduction.

[P2] [PERF-006] STRONGLY EVIDENCED WASTE tools/saipal_engine/log.py::read_events / tools/saipal.py::_load_read_only
EVIDENCE:
- `read_events()` calls `LOG.jsonl.read_bytes()`, decodes the complete file, splits the complete text into lines, JSON-parses every valid line and retains every event dict in a list.
- `_load_read_only()` needs that full list only to report `log_events=len(events)` plus `malformed_log_lines`.
- LOG is append-only, so this status cost has no natural upper bound over project lifetime.
- Synthetic 100,000-event LOG of 10,688,895 bytes processed through the real `read_events()` took 1.0342s and reached 97.73MB traced peak allocation, despite the caller ultimately needing only two integer counts.
- Current LOG is only 4,846 bytes / 13 events, so the pathological cost has not yet accumulated in this archive.
ISSUE:
A constant-size status answer materializes the complete lifetime event log in memory. Long-running unattended use turns a cheap observability command into an O(total-log-bytes) allocation spike and risks avoidable latency/RSS growth.
OPTIMIZE:
- Add a streaming `log_stats()` path that reads the file incrementally and maintains only valid/malformed counters.
- Use it from `status`/`next` surfaces that do not need event bodies.
- Keep `read_events()` for consumers that genuinely require records, or add bounded iterator/tail APIs for those callers.
- If bounded-latency status is later required, add a separately checksummed append checkpoint/counter that can be verified against file size/offset; do not make an unverifiable counter replace the append-only log.
GUARDRAIL:
- Preserve exact malformed-line counting and UTF-8 replacement behavior.
- Never repair, truncate or rewrite LOG while scanning it.
- A torn final line must remain evidence, not be silently normalized.
- CORE-008's backward sequence-recovery repair is independent and must remain intact.
VERIFY:
1. 100k-event status-stat scan uses bounded memory independent of LOG size.
2. Valid/malformed counts exactly match `read_events()` on mixed fixtures.
3. Missing/empty LOG behavior remains unchanged.
4. Add 10MB/50MB scaling check for memory; peak working memory should stay approximately constant.
5. `read_events()` callers that actually require event bodies remain behavior-compatible.

PERFORMANCE_DONE_WHEN: PERF-001..PERF-006 have regression/performance controls; detector/analyst scaling is near-linear on proportional episode streams; unchanged canonical inbox evidence is not reparsed/rehashed canonically every cycle; unchanged HOT sources avoid full normalization and appended HOT sources process verified tails without weakening prefix-conflict detection; session state is dirty-written and materially compacted without changing historical episode/evidence semantics; recurrence performs one atomic load/save per logical batch; status does not materialize the lifetime LOG; W2-001/W2-003/CORE-003/W2-006/W2-007 correctness invariants remain green; `tools/validate.py` remains >=270/270; compileall passes; and the complete canonical test runner finishes successfully.

---
ALL_3_DONE_WHEN: 3/3 waves validated and combined into canonical handoff package.